#version 420 core
layout(location = 0) out vec4 output;
layout(binding = 0) uniform sampler2D colorbuffer;
uniform uvec2 viewport;

void main (void)
{
	vec2 coord = gl_FragCoord.xy / viewport;

	vec4 color;
	color = texture2D (colorbuffer, coord);

	output = color;
}
