#version 420 core
layout(location = 0) out vec4 color;
layout(binding = 0) uniform sampler2D diffusebuffer;
uniform uvec2 viewport;

void main (void)
{
	vec4 diffuse;
	vec2 coord;
	coord = gl_FragCoord.xy / viewport;

	diffuse = texture2D (diffusebuffer, coord);
	color = diffuse;
}
