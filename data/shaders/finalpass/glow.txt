#version 420 core
layout(location = 0) out vec4 color;
layout(binding = 0) uniform sampler2D tex;
uniform uvec2 viewport;

void main (void)
{
	vec2 coord;
	coord = gl_FragCoord.xy / viewport;

	color = texture (tex, coord, 1);
//	color = texelFetch (tex, ivec2 (gl_FragCoord.xy), 1);
}
